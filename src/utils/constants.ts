export enum ItemState {
  required = 'required',
  ongoing = 'ongoing',
  complete = 'complete',
}

export enum TextVariant {
  header = 'header',
  button = 'button',
  body = 'body',
  label = 'label',
}
